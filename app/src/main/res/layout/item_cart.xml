<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.lock.smartlocker.ui.cart.CartViewModel" />

        <variable
            name="cartItem"
            type="com.lock.smartlocker.data.models.CartItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_rectangle_rounded"
        android:paddingVertical="@dimen/dp_15"
        android:paddingHorizontal="@dimen/dp_10"
        android:layout_marginVertical="@dimen/dp_5">

        <TextView
            android:id="@+id/tvItemName"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:fontFamily="@font/segoe_ui_regular"
            android:text="@{cartItem.modelName}"
            android:textSize="@dimen/sp_12"
            app:layout_constraintWidth_percent="0.333"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/tvQuantity"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvQuantity"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="20sp"
            android:text="@{@string/cart_item_quantity_format(cartItem.quantity)}"
            android:fontFamily="@font/segoe_ui_regular"
            app:layout_constraintWidth_percent="0.333"
            app:layout_constraintBottom_toBottomOf="@id/btnDecrease"
            app:layout_constraintEnd_toStartOf="@+id/btnDecrease"
            app:layout_constraintStart_toEndOf="@+id/tvItemName"
            app:layout_constraintTop_toTopOf="@id/btnDecrease" />

        <Button
            android:id="@+id/btnIncrease"
            style="@style/CustomButton"
            android:layout_marginEnd="15dp"
            android:text="+"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btnDecrease"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btnDecrease"
            style="@style/CustomButton"
            android:text="-"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>